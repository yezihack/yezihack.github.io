好的，下面我们从 **MongoDB 的全景视角** 出发，系统梳理它的各个关键方面，方便你全面理解、后续高效运维及监控优化。

---

## 一、什么是 MongoDB？

**MongoDB** 是一个基于文档的、开源的、面向集合的 NoSQL 数据库，数据以 **BSON（二进制 JSON）格式** 存储，强调灵活性、高可用和水平扩展能力。

* 类似 JSON 的结构，但支持更多类型（如 Date、Binary 等）
* 不需要事先定义表结构（schema-less），更适合快速迭代的项目
* 写入性能高、适合处理海量数据

---

## 二、核心特点

| 类型   | 内容                             |
| ---- | ------------------------------ |
| 数据模型 | 面向文档，结构灵活                      |
| 查询能力 | 支持丰富的查询操作，索引、多字段、聚合管道等         |
| 事务   | 4.0+ 支持多文档事务（副本集），4.2+ 支持分片集事务 |
| 高可用  | 原生支持 Replica Set（副本集）          |
| 水平扩展 | 支持分片（Sharding）架构，便于水平扩展        |
| 内置工具 | 支持备份、恢复、监控（Ops Manager）、CLI 等  |
| 存储引擎 | 默认 WiredTiger，支持压缩、并发控制等       |

---

## 三、MongoDB 架构

### 1. 单实例架构

仅一个 mongod 进程，适合开发或轻量业务场景。无高可用、扩展能力。

```
Client --> mongod
```

---

### 2. 副本集架构（Replica Set）

MongoDB 的高可用基本单元。

```
        +-------------------+
        |    Client         |
        +-------------------+
             |  |
             |  |
       +-----+  +------+
       |              |
   Primary       Secondary(s)
       |              |
   Oplog同步         读操作（可选）
```

> 最少三个节点，包含一个 Primary 和多个 Secondary，通过 Oplog 实现数据同步和 failover。

---

### 3. 分片集群架构（Sharded Cluster）

用于水平扩展大规模数据。

```
            +-------------+
            |   Client    |
            +-------------+
                   |
                 mongos（路由）
                   |
       +-----------+-----------+
       |                       |
Config Server           Config Server
       |                       |
  +---------+           +----------+
  | Shard 1 |           | Shard 2  |
  |(Replica)|           |(Replica) |
  +---------+           +----------+
```

* **mongos**：路由进程，请求分发器
* **Config Server**：存储分片元数据
* **Shard**：每个分片都是副本集

---

## 四、配置管理要点（基础参数）

| 参数名                                   | 说明                |
| ------------------------------------- | ----------------- |
| `storage.dbPath`                      | 数据目录路径            |
| `net.port`                            | 默认27017           |
| `security.authorization`              | 是否开启认证            |
| `replication.replSetName`             | 副本集名称             |
| `sharding.clusterRole`                | shard 或 configsvr |
| `wiredTiger.engineConfig.cacheSizeGB` | 内存缓存上限            |
| `systemLog.path`                      | 日志路径              |

👉 推荐使用 YAML 格式的配置文件 `/etc/mongod.conf`

---

## 五、性能优化建议（精华版）

### 1. 内存配置

* 保证 `wiredTiger.cacheSizeGB` 设置合理（不超过物理内存 50%\~60%）
* 副本集建议关闭 `readConcern: majority` 提升读性能（业务允许情况下）

### 2. 索引优化

* 为查询、排序字段建立复合索引
* 避免全表扫描，可使用 `$hint` 强制索引
* 使用 `explain()` 分析慢查询

### 3. 写入优化

* 批量写入 `insertMany`
* 降低 writeConcern（如 `w:1`）

### 4. 查询优化

* 合理分页：使用 `_id` 或 `range` 替代 skip/limit
* 避免 `$or` 查询过多字段

---

## 六、部署方式详解

| 部署方式 | 适用场景 | 优点         | 缺点         |
| ---- | ---- | ---------- | ---------- |
| 单实例  | 开发测试 | 简单快速       | 无高可用       |
| 副本集  | 生产必备 | 高可用、故障自动切换 | 节点成本略高     |
| 分片集群 | 大数据量 | 支持水平扩展     | 架构复杂，需运维投入 |

---

## 七、监控方案与图表建议

### 🔧 使用 Percona MongoDB Exporter + Prometheus + Grafana

### 关键指标：

| 类型    | 指标名                                      | 含义                             |
| ----- | ---------------------------------------- | ------------------------------ |
| 资源    | `mongodb_mongod_uptime_seconds`          | MongoDB 运行时间                   |
| 连接    | `mongodb_connections{state=}`            | 活跃连接数                          |
| 内存    | `mongodb_memory_resident_bytes`          | 常驻内存                           |
| 存储    | `mongodb_mongod_storage_data_size_bytes` | 数据占用空间                         |
| 副本集   | `mongodb_replset_my_state`               | 副本状态（1\:Primary, 2\:Secondary） |
| 读写    | `mongodb_op_counters_*`                  | CRUD 操作数                       |
| Oplog | `mongodb_oplog_head_timestamp_seconds`   | Oplog 头部时间戳                    |
| 慢查    | `mongodb_mongod_asserts_total`           | 错误断言次数                         |

### 推荐图表模块：

1. **节点状态面板**：副本状态/角色切换
2. **连接趋势图**：观察连接突增
3. **写入延迟图**：Insert、Update、Delete
4. **Oplog 时间延迟图**：主从同步健康度
5. **存储与磁盘趋势图**：监控空间膨胀
6. **慢查询列表面板**：通过日志或 Exporter 曝露

---

## 八、总结

| 模块   | 要点                       |
| ---- | ------------------------ |
| 数据模型 | 文档型，灵活强大                 |
| 架构   | 支持单实例、副本集、分片             |
| 配置   | 支持 YAML，参数丰富             |
| 优化   | 索引+查询策略+合理事务             |
| 运维   | 支持高可用、自动切换               |
| 监控   | Prometheus + Grafana 是首选 |

---

如果你希望我针对每一部分 **出图 + 实战 YAML + PromQL 语句 + 运维建议**，可以告诉我你当前 MongoDB 的版本、部署方式和监控环境，我将逐模块拆解给你 🔍。

是否需要我先从 **副本集部署 YAML + 监控面板设计 + PromQL** 开始？或者其他方向？
